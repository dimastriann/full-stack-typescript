# Use the direct backend URL during local Vite development. In the production
# container, Nginx proxies relative GraphQL and REST paths to the backend.
VITE_DEVELOPMENT=true
VITE_API_URL=http://localhost:3000

# Enable browser console logging while diagnosing local issues.
VITE_LOGGER_ENV=false
